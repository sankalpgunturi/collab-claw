---
name: end
description: End the current collab-claw room. Use only when the user explicitly types /collab-claw:end. Runs `bin/end` via Bash and prints output verbatim.
disable-model-invocation: true
allowed-tools: Bash(end *)
---

# End the room

Run the `end` script using the Bash tool. Print its stdout verbatim. Do not paraphrase.
