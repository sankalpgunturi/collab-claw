---
name: status
description: Show the current collab-claw room status (member list, join URL). Use only when the user explicitly types /collab-claw:status. Runs `bin/status` via Bash and prints output verbatim.
disable-model-invocation: true
allowed-tools: Bash(status *)
---

# Status

Run the `status` script using the Bash tool. Print its stdout verbatim. Do not paraphrase.
