---
name: status
description: Show the current collab-claw room status (member list, join URL). Use only when the user explicitly types /collab-claw:status. Runs `collab-claw-status` via Bash and prints output verbatim.
disable-model-invocation: true
allowed-tools: Bash(collab-claw-status:*)
---

# Status

Run the command `collab-claw-status` (no arguments) using the Bash tool. Print its stdout verbatim. Do not paraphrase.
